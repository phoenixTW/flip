Language Syntax
---------------

sample.flip
===========

var x = 10;
var y = 20;
var z = add(x, y);
print(z);

Run:

flip sample.flip

Output:

sample.js

You can run: node `sample.js` to execute that .js file
